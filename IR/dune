(library
    (name IR)
    (libraries Syntax Semantics)
    (preprocess(pps ppx_deriving.show))
    (modules ANF)
    )

(test
    (name Playground)
    (libraries Syntax Semantics IR)
    (preprocess(pps ppx_deriving.show))
    (modules Playground))
