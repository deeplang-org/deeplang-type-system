// Some top-level declarations
let mmzk: f64 = 3.14;
type Shape [
  Rectangle(width : U32, height : U32),
  Circle(radius : U32)
]

type ColoredPoint {
    as position : Point,
    color : Color
}

fun main(x: char) {}

// Some more top-level declarations
let tsr: f64 = 2.72;
let shape: Shape = new Circle(12);
let point: Point = ColoredPoint { position : a, color : ColoredPoint { position : itmakesnosense, color : butsyntacticallycorrect } };
