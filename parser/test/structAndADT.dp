// Some top-level declarations
let mmzk: f64 = 3.14;
type Shape [
  Rectangle(width : u32, height : u32),
  Circle(radius : u32),
  Nothing
]

type ColoredPoint {
    as position : Point,
    color : Color
}

fun main(x: char) {}

fun niam(x: char) {
  return 3;
}

// Some more top-level declarations
let tsr: f64 = 2.72;
let typeTShape: Shape = Circle(12);
let point: Point = ColoredPoint { position : mut a, color : ColoredPoint { position : itmakesnosense, color : butsyntacticallycorrect } };
