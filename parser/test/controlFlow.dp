fun main(x : char, y : bool) {
  let mut f : bool;
  let c : char = 'c';
  f = true;

  // meaningless if structure
  if (f) {
    print(1);
  } else if (true) {
    print(f);
    if (false) {
      foo();
    } else if (bar) {
      baz();
    }
  } else {
    fooBar();
  }

  // meaningless while structure
  let mut (x, b): i32 = (0, 3);
  while (1 + x == 3) {
    return (0, (1.2, 'c'), "Abc");
  }

  // meaningless for structures
  for (df: i32 in someThing) {
    return (0, (1.2, 'c'), "Abc");
  }
  for ((key, value) in someThing) {
    return (0, (1.2, 'c'), "Abc");
  }
}
