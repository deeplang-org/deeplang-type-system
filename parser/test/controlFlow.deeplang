fun main(x: char) {
  let f: bool;

  // meaningless if structure
  if (f) {
    print(1);
  } else if (true) {
    print(f);
    if (false) {
      foo();
    } else if (bar) {
      baz();
    }
  } else {
    fooBar();
  }

  // meaningless while structure
  let mut x: i32 = 0;
  while (1 + x == 3) {
    return (0, (1.2, 'c'), "Abc");
  }

  // meaningless for structure
  for (let mut i: i32 = 0; i < 114514; i += 1) {
    for (let j: i32 = 0; j < i;) {
      j += 1;
      j += j *= 2;
  }
  }
}
