(library
    (name Semantics)
    (libraries Syntax)
    (modules ANF Table Helper Walker)
    )

(test
    (name WalkerTest)
    (libraries Syntax Semantics)
    (deps (file test/type.dp))
    (modules WalkerTest)
    )
